server:
  port: 8080

spring:
  application:
    name: crud_cliente
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jmx:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/banco_cliente?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: password